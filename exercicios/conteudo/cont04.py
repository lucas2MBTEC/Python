#para importar as bibiçhotecas e funcionalidades em python utilizase o comando import seguido do nome da bibilhoteca oumódulo que irá 3ser carregado esse programa.
#para importar coisas específicas de uma bibilhoteca utilaza-se o comando from seguiido  o nome da bilhoteca seguido da palavra import seguido do item que desewja importar.
#import math 
#num01= int(input(' digite um numero: ')) 
#raiz= math.sqrt(num01)
#print('a raiz de {} é igual a {}'.format(num01,math.ceil raiz)) # aredpnda pra cima
#Print('a raiz de {} é igual a {}'.format(num01,math.floor raiz))# aredomda pra baixo

#importando funcionalidades expesificas.
from math import sqrt, floor,
num = int(input(''))
raiz = sqrt(num)